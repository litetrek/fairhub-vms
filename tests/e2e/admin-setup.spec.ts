import { test } from '@playwright/test'

test.describe('Admin event setup hub', () => {
  test.fixme('admin can create a new event', async () => {})
  test.fixme('admin can add booth types on the setup hub', async () => {})
  test.fixme('admin can add add-ons to an event', async () => {})
  test.fixme('admin can add event weeks', async () => {})
  test.fixme('admin can configure document requirements per booth type', async () => {})
})
