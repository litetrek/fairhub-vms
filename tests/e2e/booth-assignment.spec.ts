import { test } from '@playwright/test'

test.describe('Booth assignment', () => {
  test.fixme('staff can assign a booth number to an approved application', async () => {})
  test.fixme('assigning a booth shows the assignment on the application detail page', async () => {})
  test.fixme('staff can reassign a booth to a different number', async () => {})
})
