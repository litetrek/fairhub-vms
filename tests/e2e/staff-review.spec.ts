import { test } from '@playwright/test'

test.describe('Staff review queue', () => {
  test.fixme('staff user sees /staff/queue with submitted applications', async () => {})
  test.fixme('staff can approve an application', async () => {})
  test.fixme('staff can reject an application with a rejection note', async () => {})
  test.fixme('vendor can see rejection note and resubmit', async () => {})
})
