import { describe, it } from 'vitest'

describe('email lib', () => {
  it.todo('sendInvoiceEmail resolves with success when Resend is configured')
  it.todo('sendInvoiceEmail returns error when invoice not found')
  it.todo('sendInvoiceEmail no-ops gracefully when RESEND_API_KEY is not set')
})
