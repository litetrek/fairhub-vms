import { describe, it } from 'vitest'

describe('admin events API', () => {
  it.todo('POST creates an event with required fields')
  it.todo('PATCH updates event details')
  it.todo('GET returns all events for admin')
  it.todo('returns 403 when caller is not ADMIN')
})
