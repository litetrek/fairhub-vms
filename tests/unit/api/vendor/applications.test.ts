import { describe, it } from 'vitest'

describe('vendor applications API', () => {
  it.todo('POST creates a DRAFT application for authenticated vendor')
  it.todo('PATCH submits application and sets status to SUBMITTED')
  it.todo('GET returns vendor applications filtered by vendorId')
  it.todo('returns 401 for unauthenticated requests')
})
